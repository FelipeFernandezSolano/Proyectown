# Copia este archivo como backend/application-local.properties
# (ese nombre SI esta en .gitignore - nunca se sube al repositorio).
# Ahi coloca tus credenciales reales. Todo lo de aqui abajo es opcional excepto MySQL.

# ---- MySQL (obligatorio) ----
spring.datasource.username=root
spring.datasource.password=TU_PASSWORD_DE_MYSQL

# ---- Google Sign-In (opcional - boton "Iniciar sesion con Google") ----
# Client ID y Client Secret de Google Cloud Console > Credenciales > ID de cliente de OAuth.
# app.google.client-id=tu-client-id.apps.googleusercontent.com
# app.google.client-secret=tu-client-secret

# ---- Correo SMTP (opcional - "olvide mi contrasena") ----
# spring.mail.username=tu_correo@gmail.com
# spring.mail.password=tu_app_password_de_16_caracteres

# ---- Chatbot con IA (opcional - asistente del landing) ----
# API key gratuita en https://aistudio.google.com/apikey
# app.gemini.api-key=tu_api_key
